import { routeConstants } from '../../../../shared/constants';
import PublicTransportForm from "./";

export default {
    path: routeConstants.PUBLICTRANSPORT.route,
    component: PublicTransportForm
};